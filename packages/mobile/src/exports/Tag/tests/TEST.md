# Tag 测试概要

> **注意**：该组件未从 `@fexd/mobile` 主入口导出，属于开发中/内部组件。

## 覆盖率

| 指标       | 值   |
| ---------- | ---- |
| Statements | 100% |
| Branches   | 100% |
| Functions  | 100% |
| Lines      | 100% |

## 测试场景

1. **冒烟**：无 props 渲染固定文案「Tag」且不崩溃。
2. **导出**：`prefix` 与 `exd-tag` 一致。
3. **透传**：`className`、`style`、`data-*` 出现在根 `div`。
4. **ref**：`ref` 指向根 DOM。

## 评分

- 交互覆盖：3/5（展示型组件，无业务交互链）
- 分支覆盖：5/5（源码分支均已覆盖）
- 边界处理：4/5（透传与 ref）
- 场景真实度：4/5（与占位实现一致）
- 综合评分：16/20

## 未覆盖说明（如有）

无。
